#include <windows.h>

int WINAPI wWinMain(HINSTANCE, HINSTANCE, PWSTR, int) {
  // Real wiring arrives in Task 8. This exists so the invariant check has a
  // binary to inspect from the first commit onward.
  return 0;
}
